// YOUR CODE BELOW

// Self Solution I
// Incomplete
const myMnemonic = (a,b,c,d) => {

    var word = a[0] + b[0] + c[0] + d[0];
    return word;
}

console.log(myMnemonic('Hello','Sushant','Shah','Namaste'));